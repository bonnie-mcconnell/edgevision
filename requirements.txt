fastapi==0.141.1
uvicorn[standard]==0.52.3
opencv-python-headless==4.13.0.92
numpy==2.4.4
redis==8.1.0
onnx==1.22.0
onnxconverter_common==1.16.0
onnxruntime==1.24.4
python-multipart==0.0.20  # needed for UploadFile/File(...) - POST /demo/detect
 
# Optional, only needed if you want to export a real YOLOv8n model locally:
# ultralytics==8.4.123
# ultralytics depends on opencv-python so
# installing it will pull in a second GUI-enabled cv2 build alongside the one
# above. These two packages can't safely coexist installed together (their
# files can clash depending on install order). After exporting the model
# uninstall opencv-python and reinstall opencv-python-headless or
# do the export in a separate throwaway venv instead of this project's.
# The Docker image handles this automatically via a separate multi-stage
# builder stage.